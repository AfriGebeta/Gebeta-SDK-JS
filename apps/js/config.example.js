// create a similar file called config.js next to this one, and replace the value of GEBETA_API_KEY with your API key from https://gebeta.app in order to run the examples
function loadConfig() {
  return {
    GEBETA_API_KEY: 'YOUR_API_KEY_HERE',
  };
}
