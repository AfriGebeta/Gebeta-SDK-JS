{
  "name": "@gebeta/react",
  "version": "0.1.0",
  "description": "React SDK for Gebeta Maps",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./clustering": {
      "import": "./dist/clustering.js",
      "types": "./dist/entries/clustering.d.ts"
    }
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "yarn workspace @gebeta/api build && yarn workspace @gebeta/core build && vite build && tsc --emitDeclarationOnly --skipLibCheck",
    "clean": "rm -rf dist",
    "lint": "test -d src && eslint \"src/**/*.{ts,tsx}\" || echo 'No src directory - skipping lint'",
    "lint:fix": "test -d src && eslint \"src/**/*.{ts,tsx}\" --fix || echo 'No src directory - skipping lint'",
    "typecheck": "test -d src && tsc --noEmit || echo 'No src directory - skipping typecheck'",
    "format": "test -d src && prettier --write \"src/**/*.{ts,tsx}\" || echo 'No src directory - skipping format'",
    "format:check": "test -d src && prettier --check \"src/**/*.{ts,tsx}\" || echo 'No src directory - skipping format check'",
    "test": "jest"
  },
  "dependencies": {
    "@gebeta/api": "workspace:*",
    "@gebeta/core": "workspace:*",
    "maplibre-gl": "^4.0.0",
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^14.0.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@vitejs/plugin-react": "^6.0.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-util": "^29.7.0",
    "react-dom": "^18.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "vite": "^8.0.14"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  }
}
