{
  "name": "@gebeta/react",
  "version": "3.0.11",
  "description": "React SDK for Gebeta Maps",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./clustering": {
      "import": "./dist/clustering.js",
      "types": "./dist/entries/clustering.d.ts"
    }
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "typedocOptions": {
    "entryPoints": [
      "src/index.ts"
    ]
  },
  "scripts": {
    "build": "yarn workspace @gebeta/api build && yarn workspace @gebeta/core build && yarn workspace @gebeta/js build && vite build && tsc --emitDeclarationOnly --skipLibCheck",
    "clean": "rm -rf dist",
    "lint": "test -d src && eslint \"src/**/*.{ts,tsx}\" || echo 'No src directory - skipping lint'",
    "lint:fix": "test -d src && eslint \"src/**/*.{ts,tsx}\" --fix || echo 'No src directory - skipping lint'",
    "typecheck": "test -d src && tsc --noEmit || echo 'No src directory - skipping typecheck'",
    "format": "test -d src && prettier --write \"src/**/*.{ts,tsx}\" || echo 'No src directory - skipping format'",
    "format:check": "test -d src && prettier --check \"src/**/*.{ts,tsx}\" || echo 'No src directory - skipping format check'",
    "test": "jest"
  },
  "dependencies": {
    "@gebeta/api": "workspace:*",
    "@gebeta/core": "workspace:*",
    "@gebeta/js": "workspace:*",
    "maplibre-gl": "^4.0.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^16.0.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@vitejs/plugin-react": "^4.2.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-util": "^29.7.0",
    "react-dom": "^19.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
