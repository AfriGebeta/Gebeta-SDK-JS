{
  "name": "@gebeta/maps-js",
  "version": "0.1.0",
  "description": "Plain JavaScript SDK for Gebeta Maps (browser)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "yarn workspace @gebeta/maps-api build && yarn workspace @gebeta/maps-core build && vite build && tsc --emitDeclarationOnly --skipLibCheck",
    "clean": "rm -rf dist",
    "lint": "test -d src && eslint \"src/**/*.{ts,js}\" || echo 'No src directory - skipping lint'",
    "lint:fix": "test -d src && eslint \"src/**/*.{ts,js}\" --fix || echo 'No src directory - skipping lint'",
    "typecheck": "test -d src && (yarn workspace @gebeta/maps-api build && yarn workspace @gebeta/maps-core build && tsc --noEmit) || echo 'No src directory - skipping typecheck'",
    "format": "test -d src && prettier --write \"src/**/*.{ts,js}\" || echo 'No src directory - skipping format'",
    "format:check": "test -d src && prettier --check \"src/**/*.{ts,js}\" || echo 'No src directory - skipping format check'"
  },
  "dependencies": {
    "@gebeta/maps-api": "workspace:*",
    "@gebeta/maps-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
