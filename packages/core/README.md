Core holds all the logic. This is where the non-platform specific code lives.

# tests

Unit tests are normal. Nothing special there. But the integration tests are run against real API. you need an API key in a config.js file in the root of the core/src folder

integration tests are only run locally, not on CI, for now.
