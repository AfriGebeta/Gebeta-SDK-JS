/* Add a file like this to the root of your project, and replace the value of apiKey with your
API key from https://gebeta.app in order to run the integration tests
* */
module.exports = {
  apiKey: 'your-api-key-here',
};
